import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { Colors } from './colors';

<Meta title="Design/Colors" component={Colors} />

export const Template = (args) => <Colors />;

# Colors

<Canvas>
  <Story name="Colors" args={{ color: 'MAIN_DARK' }}>
    {Template.bind({})}
  </Story>
</Canvas>
